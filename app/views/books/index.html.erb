<h1>Books</h1>

<% @books.each do |book| %>

  <p><%= book.title %>
    <%= book.body %>
    <%= link_to "show", book_path(book.id) %>
    <%= link_to "Edit", edit_book_path(book.id) %>
    <%= link_to "Destroy", book_path(book.id) %>
  </p>
  
  
<% end %>